name = "thepizzaboxnewyork"
main = "src/index.js"
compatibility_date = "2024-01-01"

[site]
bucket = "."

# Custom domain routing (uncomment when domain is added to Cloudflare)
# [[routes]]
# pattern = "thepizzaboxnewyork.com/*"
# zone_name = "thepizzaboxnewyork.com"

# Custom headers for better performance
[[headers]]
for = "/*"
[headers.values]
X-Frame-Options = "DENY"
X-Content-Type-Options = "nosniff"
X-XSS-Protection = "1; mode=block"
